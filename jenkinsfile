pipeline {
  agent any
  stages {
    stage('myStage'){
      steps {
        bat 'dir' 
      }
    }
    stage('Build') {
      steps { 
        bat 'echo "Hello World"' 
		bat 'dir'
      }
    }
  }
}